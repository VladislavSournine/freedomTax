# Web UI Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the Jupyter notebook with a Flask web app — upload page + results page — that runs locally with `python app.py`.

**Architecture:** Flask + Jinja2, no database, file processed in memory. All business logic in `src/` stays untouched. POST renders the result directly (no PRG session) — deliberate simplification for a single-user local tool where F5 re-submit is not a real concern.

**Tech Stack:** Python 3.9+, Flask 3.x, Jinja2 (bundled with Flask), existing `src/` modules.

---

## File Map

| File | Action | Responsibility |
|------|--------|---------------|
| `app.py` | Create | Flask routes, orchestrates src/ pipeline |
| `templates/base.html` | Create | Layout, CSS, disclaimer banner, flash messages |
| `templates/upload.html` | Create | Upload form with Freedom24 instructions |
| `templates/result.html` | Create | Two-column results + F1 table |
| `tests/test_app.py` | Create | Flask test client tests |
| `requirements.txt` | Modify | Remove jupyter/notebook/ipykernel/pandas, add flask |
| `tax_calculator.ipynb` | Delete | Replaced by web app |
| `README.md` | Modify | Update run instructions |

`src/` files: **not touched**.

---

## Chunk 1: Cleanup + Flask skeleton + upload page

### Task 1: Cleanup and update requirements

**Files:**
- Modify: `requirements.txt`
- Delete: `tax_calculator.ipynb`

- [ ] **Step 1: Update requirements.txt**

Replace the entire file content:

```
requests>=2.31
lxml>=5.0
flask>=3.0
pytest>=8.0
```

(Removed: pandas, jupyter, notebook, ipykernel. Added: flask.)

- [ ] **Step 2: Delete the notebook**

```bash
rm tax_calculator.ipynb
```

- [ ] **Step 3: Install updated dependencies**

```bash
source .venv/bin/activate
pip install -r requirements.txt
```

Expected: flask installs successfully, no errors.

- [ ] **Step 4: Verify existing tests still pass**

```bash
pytest tests/test_tax.py tests/test_report.py -v
```

Expected: 12 passed.

- [ ] **Step 5: Commit**

```bash
git add requirements.txt
git rm tax_calculator.ipynb
git commit -m "chore: replace notebook with flask, clean requirements"
```

---

### Task 2: Flask skeleton + upload page

**Files:**
- Create: `app.py`
- Create: `templates/base.html`
- Create: `templates/upload.html`
- Create: `tests/test_app.py`

- [ ] **Step 1: Write the failing test**

Create `tests/test_app.py`:

```python
import pytest
from app import app as flask_app


@pytest.fixture
def client():
    flask_app.config["TESTING"] = True
    flask_app.config["WTF_CSRF_ENABLED"] = False
    with flask_app.test_client() as c:
        yield c


def test_upload_page_loads(client):
    resp = client.get("/")
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "Freedom24" in body
    assert "JSON" in body
    assert "відкриття рахунку" in body


def test_upload_page_has_year_select(client):
    resp = client.get("/")
    body = resp.data.decode("utf-8")
    assert "2025" in body
    assert 'name="tax_year"' in body
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/test_app.py -v
```

Expected: `ModuleNotFoundError: No module named 'app'`

- [ ] **Step 3: Create app.py**

```python
import json
from datetime import date

import requests
from flask import Flask, flash, redirect, render_template, request, url_for

from src.fifo import (FIFOCalculator, enrich_dividends_with_uah,
                      enrich_other_income_with_uah,
                      enrich_withholding_taxes_with_uah)
from src.nbu import NBUClient
from src.parser import parse_freedom_json
from src.tax import calculate_taxes

app = Flask(__name__)
app.secret_key = "local-tax-calculator-dev-secret"

_CURRENT_YEAR = date.today().year
YEAR_OPTIONS = list(range(_CURRENT_YEAR, 2019, -1))  # e.g. [2026, 2025, ..., 2020]
DEFAULT_YEAR = _CURRENT_YEAR - 1


@app.route("/")
def upload():
    return render_template("upload.html", years=YEAR_OPTIONS, default_year=DEFAULT_YEAR)


@app.route("/calculate", methods=["POST"])
def calculate():
    # --- validate file ---
    f = request.files.get("file")
    if not f or f.filename == "":
        flash("Оберіть файл для завантаження")
        return redirect(url_for("upload"))

    try:
        raw = json.load(f)
    except (json.JSONDecodeError, UnicodeDecodeError):
        flash("Невірний формат файлу. Завантажте JSON звіт з Freedom24")
        return redirect(url_for("upload"))

    # --- validate year ---
    try:
        tax_year = int(request.form.get("tax_year", 0))
        if not (2020 <= tax_year <= _CURRENT_YEAR):
            raise ValueError
    except (ValueError, TypeError):
        flash("Невірний звітний рік")
        return redirect(url_for("upload"))

    # --- parse Freedom24 structure ---
    try:
        parsed = parse_freedom_json(raw)
    except (KeyError, TypeError, AttributeError):
        flash("Файл не схожий на звіт Freedom24. Перевірте що обрали правильний файл")
        return redirect(url_for("upload"))

    # --- NBU rates + FIFO ---
    try:
        nbu = NBUClient()
        positions = FIFOCalculator(nbu).calculate(parsed["trades"], tax_year)
        dividends = enrich_dividends_with_uah(parsed["dividends"], nbu)
        other_income = enrich_other_income_with_uah(parsed["other_income"], nbu)
        withholding_taxes = enrich_withholding_taxes_with_uah(
            parsed["withholding_taxes"], nbu
        )
    except requests.RequestException:
        flash("Не вдалося отримати курси НБУ. Перевірте інтернет-з'єднання")
        return redirect(url_for("upload"))

    # --- filter by tax year ---
    dividends_year = [d for d in dividends if d["date"][:4] == str(tax_year)]
    other_income_year = [i for i in other_income if i["date"][:4] == str(tax_year)]
    wht_year = [t for t in withholding_taxes if t["date"][:4] == str(tax_year)]

    # --- calculate taxes ---
    tax_result = calculate_taxes(
        positions,
        dividends_year,
        tax_year,
        other_income=other_income_year,
        withholding_taxes=wht_year,
    )

    # --- group positions by ticker for F1 table ---
    f1_groups = _group_by_ticker(positions)

    return render_template(
        "result.html",
        tax_result=tax_result,
        f1_groups=f1_groups,
        tax_year=tax_year,
        has_trades=tax_result["gross_profit_uah"] != 0.0,
        has_foreign_income=(
            tax_result["dividend_income_uah"] + tax_result["other_income_uah"] > 0
        ),
    )


def _group_by_ticker(positions: list) -> list:
    groups: dict = {}
    for pos in positions:
        t = pos["ticker"]
        if t not in groups:
            groups[t] = {"ticker": t, "proceeds_uah": 0.0,
                         "expenses_uah": 0.0, "profit_uah": 0.0}
        groups[t]["proceeds_uah"] += pos["proceeds_uah"]
        groups[t]["expenses_uah"] += (
            pos["cost_uah"] + pos["buy_commission_uah"] + pos["sell_commission_uah"]
        )
        groups[t]["profit_uah"] += pos["profit_uah"]
    return list(groups.values())


if __name__ == "__main__":
    app.run(debug=True, port=5000)
```

- [ ] **Step 4: Create templates/base.html**

```bash
mkdir -p templates
```

Create `templates/base.html`:

```html
<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Калькулятор податків Freedom24</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: system-ui, -apple-system, sans-serif; font-size: 14px;
           color: #1e293b; background: #f8fafc; line-height: 1.5; }
    .container { max-width: 960px; margin: 0 auto; padding: 24px 16px; }

    /* Flash messages */
    .flash { background: #fef2f2; border: 1px solid #fca5a5; border-radius: 6px;
             padding: 10px 14px; margin-bottom: 16px; color: #991b1b; }

    /* Disclaimer */
    .disclaimer { background: #fef9c3; border: 1px solid #fde047; border-radius: 6px;
                  padding: 10px 14px; margin-bottom: 20px; font-size: 12px; color: #713f12; }

    /* Summary bar */
    .summary-bar { background: #f0fdf4; border: 1px solid #86efac; border-radius: 8px;
                   padding: 14px 20px; margin-bottom: 20px;
                   display: flex; gap: 32px; align-items: center; }
    .summary-bar .item { display: flex; flex-direction: column; }
    .summary-bar .label { font-size: 11px; color: #6b7280; }
    .summary-bar .value { font-size: 15px; font-weight: 600; }
    .summary-bar .total .value { font-size: 18px; color: #15803d; }
    .summary-bar .sep { border-left: 1px solid #86efac; padding-left: 32px; }

    /* Two-column layout */
    .two-col { display: grid; grid-template-columns: 1fr 1.2fr; gap: 16px;
               margin-bottom: 20px; }
    @media (max-width: 680px) { .two-col { grid-template-columns: 1fr; } }

    /* Card */
    .card { background: #fff; border: 1px solid #e2e8f0; border-radius: 8px;
            overflow: hidden; margin-bottom: 10px; }
    .card-header { background: #f8fafc; padding: 7px 12px; font-size: 11px;
                   font-weight: 600; color: #475569; border-bottom: 1px solid #e2e8f0;
                   text-transform: uppercase; letter-spacing: .04em; }
    .card-header.blue { background: #eff6ff; color: #1e40af;
                        border-bottom-color: #bfdbfe; text-transform: none;
                        font-size: 12px; letter-spacing: 0; }
    .card-header.green { background: #f0fdf4; color: #15803d;
                         border-bottom-color: #bbf7d0; text-transform: none;
                         font-size: 12px; letter-spacing: 0; }

    /* Data table */
    .data-table { width: 100%; border-collapse: collapse; font-size: 12px; }
    .data-table td { padding: 5px 12px; border-bottom: 1px solid #f1f5f9; }
    .data-table tr:last-child td { border-bottom: none; }
    .data-table tr:nth-child(even) { background: #f8fafc; }
    .data-table .key { color: #6b7280; width: 55%; }
    .data-table .val { font-weight: 600; text-align: right; }
    .data-table .note { color: #6b7280; font-size: 11px; padding: 4px 12px 6px;
                        border-bottom: none; }
    .data-table .ok { color: #16a34a; font-weight: 600; }

    /* F1 table */
    .f1-section { margin-top: 4px; }
    .f1-section h3 { font-size: 13px; margin-bottom: 8px; color: #1e293b; }
    .f1-table { width: 100%; border-collapse: collapse; font-size: 12px;
                background: #fff; border: 1px solid #e2e8f0; border-radius: 8px;
                overflow: hidden; }
    .f1-table th { background: #f8fafc; padding: 7px 12px; text-align: left;
                   font-weight: 600; color: #475569; border-bottom: 1px solid #e2e8f0;
                   font-size: 11px; }
    .f1-table th.num { text-align: right; }
    .f1-table td { padding: 6px 12px; border-bottom: 1px solid #f1f5f9; }
    .f1-table tr:last-child td { border-bottom: none; font-weight: 600;
                                  background: #f8fafc; }
    .f1-table td.num { text-align: right; font-variant-numeric: tabular-nums; }
    .f1-table td.neg { color: #dc2626; }

    /* Upload form */
    .upload-wrap { max-width: 520px; margin: 0 auto; }
    .steps { background: #fff; border: 1px solid #e2e8f0; border-radius: 8px;
             overflow: hidden; margin-bottom: 16px; }
    .steps-header { background: #eff6ff; padding: 8px 14px; font-weight: 600;
                    font-size: 12px; color: #1e40af; border-bottom: 1px solid #bfdbfe; }
    .steps-body { padding: 12px 14px; }
    .step { display: flex; gap: 10px; margin-bottom: 8px; align-items: flex-start; }
    .step:last-child { margin-bottom: 0; }
    .step-num { background: #3b82f6; color: #fff; border-radius: 50%;
                width: 20px; height: 20px; display: flex; align-items: center;
                justify-content: center; font-size: 10px; font-weight: 700;
                flex-shrink: 0; margin-top: 1px; }
    .step-text { font-size: 13px; line-height: 1.5; }
    .step-warn { display: block; color: #dc2626; font-size: 11px; margin-top: 2px; }
    .form-box { background: #fff; border: 1px solid #e2e8f0; border-radius: 8px;
                padding: 14px; margin-bottom: 14px; }
    .form-group { margin-bottom: 10px; }
    .form-group:last-child { margin-bottom: 0; }
    label { display: block; font-size: 12px; font-weight: 600; color: #374151;
            margin-bottom: 4px; }
    input[type=file], select { width: 100%; border: 1px solid #d1d5db; border-radius: 5px;
                                padding: 6px 10px; font-size: 13px; background: #fff; }
    .btn { display: block; width: 100%; background: #2563eb; color: #fff;
           border: none; border-radius: 6px; padding: 10px; font-size: 14px;
           font-weight: 600; cursor: pointer; text-align: center; }
    .btn:hover { background: #1d4ed8; }
    .btn-secondary { background: #f1f5f9; color: #475569; border: 1px solid #e2e8f0;
                     text-decoration: none; display: inline-block; border-radius: 6px;
                     padding: 8px 16px; font-size: 13px; cursor: pointer; }
    .btn-secondary:hover { background: #e2e8f0; }

    /* Page title */
    .page-title { text-align: center; margin-bottom: 24px; }
    .page-title h1 { font-size: 20px; font-weight: 700; }
    .page-title p { color: #6b7280; font-size: 13px; margin-top: 4px; }

    /* Column headings */
    .col-heading { font-size: 11px; font-weight: 700; text-transform: uppercase;
                   letter-spacing: .05em; color: #94a3b8; margin-bottom: 8px; }
  </style>
</head>
<body>
  <div class="container">
    {% with messages = get_flashed_messages() %}
      {% if messages %}
        {% for msg in messages %}
          <div class="flash">{{ msg }}</div>
        {% endfor %}
      {% endif %}
    {% endwith %}
    {% block content %}{% endblock %}
  </div>
</body>
</html>
```

- [ ] **Step 5: Create templates/upload.html**

```html
{% extends "base.html" %}
{% block content %}
<div class="upload-wrap">
  <div class="page-title">
    <h1>🇺🇦 Калькулятор податків Freedom24</h1>
    <p>Розрахунок ПДФО та ВЗ для декларації про майновий стан і доходи</p>
  </div>

  <div class="steps">
    <div class="steps-header">Як отримати звіт у Freedom24</div>
    <div class="steps-body">
      <div class="step">
        <div class="step-num">1</div>
        <div class="step-text">Відкрий <strong>freedom24.com</strong> → увійди в особистий кабінет</div>
      </div>
      <div class="step">
        <div class="step-num">2</div>
        <div class="step-text">Перейди в <strong>Мій рахунок → Звіти → Загальний звіт</strong></div>
      </div>
      <div class="step">
        <div class="step-num">3</div>
        <div class="step-text">
          Формат: <strong>JSON</strong>&nbsp;&nbsp;·&nbsp;&nbsp;
          Період: <strong>від дати відкриття рахунку</strong> до <strong>31 грудня</strong> звітного року
          <span class="step-warn">⚠️ Важливо: повна історія потрібна для правильного FIFO-розрахунку.
            Якщо взяти тільки поточний рік — купівлі попередніх років не потраплять до розрахунку.</span>
        </div>
      </div>
      <div class="step">
        <div class="step-num">4</div>
        <div class="step-text">Завантаж отриманий файл нижче та вкажи звітний рік</div>
      </div>
    </div>
  </div>

  <form method="POST" action="/calculate" enctype="multipart/form-data">
    <div class="form-box">
      <div class="form-group">
        <label for="file">Файл звіту (JSON)</label>
        <input type="file" id="file" name="file" accept=".json" required>
      </div>
      <div class="form-group">
        <label for="tax_year">Звітний рік</label>
        <select id="tax_year" name="tax_year">
          {% for year in years %}
            <option value="{{ year }}"
              {% if year == default_year %}selected{% endif %}>{{ year }}</option>
          {% endfor %}
        </select>
      </div>
    </div>
    <button type="submit" class="btn">Розрахувати →</button>
  </form>
</div>
{% endblock %}
```

- [ ] **Step 6: Run tests to verify they pass**

```bash
pytest tests/test_app.py::test_upload_page_loads tests/test_app.py::test_upload_page_has_year_select -v
```

Expected: 2 passed.

- [ ] **Step 7: Verify manually**

```bash
python app.py
```

Open http://localhost:5000 — повинна з'явитись сторінка завантаження з 4 кроками та формою.

- [ ] **Step 8: Commit**

```bash
git add app.py templates/base.html templates/upload.html tests/test_app.py
git commit -m "feat: add Flask skeleton and upload page"
```

---

## Chunk 2: Calculate route + result page

### Task 3: Result page template + integration test

**Files:**
- Create: `templates/result.html`
- Modify: `app.py` (add enumerate filter)
- Modify: `tests/test_app.py` (add integration tests)

> Note on result page structure: the right column has a **Step 3 — Ф1** card that shows
> the field codes to enter. The **F1 detail table** (ticker-by-ticker rows) appears separately
> below both columns. Both are conditionally shown only when `has_trades` is true.

- [ ] **Step 1: Write the integration tests**

Add to `tests/test_app.py`:

```python
import io
import json
from unittest.mock import MagicMock, patch


def _minimal_freedom_json(tax_year: int) -> dict:
    """Minimal valid Freedom24 JSON with one dividend in tax_year."""
    return {
        "trades": {"detailed": []},
        "cash_flows": {
            "detailed": [
                {
                    "type_id": "dividend",
                    "date": f"{tax_year}-06-15",
                    "amount": "100.00",
                    "currency": "USD",
                    "comment": (
                        f"Dividends on security (Apple Inc (AAPL.US)), "
                        f"accrual date {tax_year}-06-15"
                    ),
                }
            ]
        },
    }


@patch("app.NBUClient")
def test_calculate_renders_result(mock_nbu_cls, client):
    # NBU rate = 40.00 UAH/USD (pinned)
    # 100 USD * 40.00 = 4000.00 UAH dividend
    # No withholding tax in fixture → wht_credit_uah = 0 → full 18% applies
    # pdfo_dividends = 4000.00 * 0.18 = 720.00 UAH
    # vz_dividends (2025, 5%) = 4000.00 * 0.05 = 200.00 UAH
    # pdfo_total = 720.00, vz_total = 200.00, total = 920.00
    mock_nbu = MagicMock()
    mock_nbu.get_rate.return_value = 40.00
    mock_nbu_cls.return_value = mock_nbu

    data = json.dumps(_minimal_freedom_json(2025)).encode()
    resp = client.post(
        "/calculate",
        data={"file": (io.BytesIO(data), "report.json"), "tax_year": "2025"},
        content_type="multipart/form-data",
    )
    assert resp.status_code == 200
    body = resp.data.decode("utf-8")
    assert "2025" in body
    assert "ПДФО" in body
    assert "До сплати" in body
    assert "720.00" in body   # pdfo_dividends (18%, no WHT credit)
    assert "200.00" in body   # vz_dividends (5%)
    assert "920.00" in body   # total to pay


def test_calculate_no_file_shows_flash(client):
    resp = client.post("/calculate", data={"tax_year": "2025"},
                       content_type="multipart/form-data")
    assert resp.status_code == 302
    # Follow redirect and check flash message rendered
    resp2 = client.get(resp.headers["Location"])
    assert "Оберіть файл" in resp2.data.decode("utf-8")


def test_calculate_invalid_json_shows_flash(client):
    resp = client.post(
        "/calculate",
        data={"file": (io.BytesIO(b"not json"), "bad.json"), "tax_year": "2025"},
        content_type="multipart/form-data",
    )
    assert resp.status_code == 302
    resp2 = client.get(resp.headers["Location"])
    assert "Невірний формат" in resp2.data.decode("utf-8")


def test_calculate_invalid_year_shows_flash(client):
    data = json.dumps({"trades": {"detailed": []},
                       "cash_flows": {"detailed": []}}).encode()
    resp = client.post(
        "/calculate",
        data={"file": (io.BytesIO(data), "r.json"), "tax_year": "1999"},
        content_type="multipart/form-data",
    )
    assert resp.status_code == 302
    resp2 = client.get(resp.headers["Location"])
    assert "Невірний звітний рік" in resp2.data.decode("utf-8")
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/test_app.py::test_calculate_renders_result -v
```

Expected: FAIL — `TemplateNotFound: result.html`

- [ ] **Step 3: Create templates/result.html**

```html
{% extends "base.html" %}
{% block content %}

<div class="disclaimer">
  ⚠️ <strong>Дисклеймер:</strong> Цей інструмент надається виключно для довідки.
  Перевірте розрахунки самостійно або з податковим консультантом перед поданням
  декларації. Автор не несе відповідальності за помилки у розрахунках.
</div>

<!-- Summary bar -->
<div class="summary-bar">
  <div class="item">
    <span class="label">ПДФО</span>
    <span class="value">{{ "%.2f"|format(tax_result.pdfo_total) }} грн</span>
  </div>
  <div class="item">
    <span class="label">ВЗ</span>
    <span class="value">{{ "%.2f"|format(tax_result.vz_total) }} грн</span>
  </div>
  <div class="item sep total">
    <span class="label">До сплати</span>
    <span class="value">
      {{ "%.2f"|format(tax_result.pdfo_total + tax_result.vz_total) }} грн
    </span>
  </div>
</div>

<div class="two-col">

  <!-- LEFT: Calculation breakdown -->
  <div>
    <div class="col-heading">Розрахунок</div>

    <!-- Dividends -->
    <div class="card">
      <div class="card-header">Дивіденди</div>
      <table class="data-table">
        <tr>
          <td class="key">Сума доходу</td>
          <td class="val">{{ "%.2f"|format(tax_result.dividend_income_uah) }} грн</td>
        </tr>
        <tr>
          <td class="key">ПДФО{% if tax_result.wht_credit_uah > 0 %} (9% ефективна){% else %} (18%){% endif %}</td>
          <td class="val">{{ "%.2f"|format(tax_result.pdfo_dividends) }} грн</td>
        </tr>
        <tr>
          <td class="key">ВЗ</td>
          <td class="val">{{ "%.2f"|format(tax_result.vz_dividends) }} грн</td>
        </tr>
      </table>
    </div>

    <!-- Cashback -->
    <div class="card">
      <div class="card-header">Cashback (scores_rebate)</div>
      <table class="data-table">
        <tr>
          <td class="key">Сума доходу</td>
          <td class="val">{{ "%.2f"|format(tax_result.other_income_uah) }} грн</td>
        </tr>
        <tr>
          <td class="key">ПДФО (18%)</td>
          <td class="val">{{ "%.2f"|format(tax_result.pdfo_other) }} грн</td>
        </tr>
        <tr>
          <td class="key">ВЗ</td>
          <td class="val">{{ "%.2f"|format(tax_result.vz_other) }} грн</td>
        </tr>
      </table>
    </div>

    <!-- Trades -->
    <div class="card">
      <div class="card-header">Торгівля ЦП</div>
      <table class="data-table">
        <tr>
          <td class="key">Закритих позицій</td>
          <td class="val">{{ f1_groups | length }}</td>
        </tr>
        {% if has_trades %}
        <tr>
          <td class="key">Валовий прибуток</td>
          <td class="val">{{ "%.2f"|format(tax_result.gross_profit_uah) }} грн</td>
        </tr>
        <tr>
          <td class="key">Чистий прибуток (база)</td>
          <td class="val">{{ "%.2f"|format(tax_result.net_profit_uah) }} грн</td>
        </tr>
        <tr>
          <td class="key">ПДФО (18%)</td>
          <td class="val">{{ "%.2f"|format(tax_result.pdfo_trades) }} грн</td>
        </tr>
        <tr>
          <td class="key">ВЗ</td>
          <td class="val">{{ "%.2f"|format(tax_result.vz_trades) }} грн</td>
        </tr>
        {% else %}
        <tr>
          <td class="key">Додаток Ф1</td>
          <td class="val ok">не потрібен</td>
        </tr>
        {% endif %}
      </table>
    </div>
  </div>

  <!-- RIGHT: Form-filling instruction -->
  <div>
    <div class="col-heading">Що вписати в декларацію</div>

    <!-- Step 1: Row 10.10 -->
    {% set foreign_total = tax_result.dividend_income_uah + tax_result.other_income_uah %}
    <div class="card">
      <div class="card-header blue">Крок 1 — Розділ II, рядок 10.10</div>
      <table class="data-table">
        <tr>
          <td class="key">r01010g2s&nbsp;країна/валюта</td>
          <td class="val">США, долар США (USD)</td>
        </tr>
        <tr>
          <td class="key">r01010g3&nbsp;сума доходу</td>
          <td class="val">{{ "%.2f"|format(foreign_total) }}</td>
        </tr>
        <tr>
          <td class="key">r01010g6&nbsp;ПДФО</td>
          <td class="val">{{ "%.2f"|format(tax_result.pdfo_dividends + tax_result.pdfo_other) }}</td>
        </tr>
        <tr>
          <td class="key">r01010g7&nbsp;ВЗ</td>
          <td class="val">{{ "%.2f"|format(tax_result.vz_dividends + tax_result.vz_other) }}</td>
        </tr>
        <tr>
          <td class="note" colspan="2">
            ↳ для довідки: дивіденди {{ "%.2f"|format(tax_result.dividend_income_uah) }}
            + cashback {{ "%.2f"|format(tax_result.other_income_uah) }}
          </td>
        </tr>
      </table>
    </div>

    <!-- Step 2: Row 10.8 -->
    <div class="card">
      <div class="card-header blue">Крок 2 — Рядок 10.8 (інвестиційний прибуток)</div>
      {% if tax_result.net_profit_uah > 0 %}
      <table class="data-table">
        <tr>
          <td class="key">r0108g3&nbsp;сума прибутку</td>
          <td class="val">{{ "%.2f"|format(tax_result.net_profit_uah) }}</td>
        </tr>
        <tr>
          <td class="key">r0108g6&nbsp;ПДФО</td>
          <td class="val">{{ "%.2f"|format(tax_result.pdfo_trades) }}</td>
        </tr>
        <tr>
          <td class="key">r0108g7&nbsp;ВЗ</td>
          <td class="val">{{ "%.2f"|format(tax_result.vz_trades) }}</td>
        </tr>
        <tr>
          <td class="note" colspan="2">Заповнюється автоматично з Додатку Ф1</td>
        </tr>
      </table>
      {% elif has_trades %}
      <table class="data-table">
        <tr>
          <td class="key">Результат</td>
          <td class="val ok">Збиток — рядок 10.8 = 0</td>
        </tr>
        <tr>
          <td class="note" colspan="2">Ф1 все одно заповнюється (для фіксації збитку)</td>
        </tr>
      </table>
      {% else %}
      <table class="data-table">
        <tr>
          <td class="key ok" colspan="2">✓ Продажів не було — рядок не заповнюється</td>
        </tr>
      </table>
      {% endif %}
    </div>

    <!-- Step 3: F1 -->
    <div class="card">
      <div class="card-header {% if has_trades %}blue{% else %}green{% endif %}">
        Крок 3 — Додаток Ф1
      </div>
      {% if has_trades %}
      <table class="data-table">
        <tr>
          <td class="key">t1rxxxxg2</td>
          <td class="val" style="font-size:11px">Інвестиційні активи з джерел за межами України</td>
        </tr>
        <tr>
          <td class="key">t1rxxxxg3s</td>
          <td class="val">назва тікера (BAC.US, JNJ.US…)</td>
        </tr>
        <tr>
          <td class="key">t1rxxxxg4</td>
          <td class="val">сума доходу від продажу (грн)</td>
        </tr>
        <tr>
          <td class="key">t1rxxxxg5</td>
          <td class="val">витрати + комісії (грн)</td>
        </tr>
        <tr>
          <td class="key">t1rxxxxg6</td>
          <td class="val">фін. результат (від'ємне зі знаком −)</td>
        </tr>
        <tr>
          <td class="note" colspan="2">Дивись таблицю нижче ↓</td>
        </tr>
      </table>
      {% else %}
      <table class="data-table">
        <tr>
          <td class="ok" colspan="2" style="padding:8px 12px">
            ✓ Ф1 не подається — продажів не було
          </td>
        </tr>
      </table>
      {% endif %}
    </div>

  </div><!-- /right col -->
</div><!-- /two-col -->

<!-- F1 detail table (only if there were trades) -->
{% if has_trades %}
<div class="f1-section">
  <h3>Таблиця для Додатку Ф1 — {{ tax_year }} рік</h3>
  <table class="f1-table">
    <thead>
      <tr>
        <th style="width:32px">#</th>
        <th>Найменування (t1rxxxxg3s)</th>
        <th class="num">Дохід г4 (грн)</th>
        <th class="num">Витрати г5 (грн)</th>
        <th class="num">Результат г6 (грн)</th>
      </tr>
    </thead>
    <tbody>
      {% for i, g in f1_groups | enumerate(start=1) %}
      <tr>
        <td>{{ i }}</td>
        <td>{{ g.ticker }}</td>
        <td class="num">{{ "%.2f"|format(g.proceeds_uah) }}</td>
        <td class="num">{{ "%.2f"|format(g.expenses_uah) }}</td>
        <td class="num {% if g.profit_uah < 0 %}neg{% endif %}">
          {{ "%.2f"|format(g.profit_uah) }}
        </td>
      </tr>
      {% endfor %}
      <tr>
        <td></td>
        <td>УСЬОГО</td>
        <td class="num">{{ "%.2f"|format(f1_groups | sum(attribute='proceeds_uah')) }}</td>
        <td class="num">{{ "%.2f"|format(f1_groups | sum(attribute='expenses_uah')) }}</td>
        <td class="num">{{ "%.2f"|format(f1_groups | sum(attribute='profit_uah')) }}</td>
      </tr>
    </tbody>
  </table>
  <p style="font-size:11px;color:#6b7280;margin-top:6px">
    Від'ємний фінансовий результат вводиться зі знаком "−" у cabinet.tax.gov.ua
  </p>
</div>
{% endif %}

<div style="margin-top:24px;padding-top:16px;border-top:1px solid #e2e8f0">
  <a href="/" class="btn-secondary">← Розрахувати інший файл</a>
</div>

{% endblock %}
```

- [ ] **Step 4: Add `enumerate` filter to app.py**

Jinja2 doesn't have a built-in `enumerate`. Add a custom filter in `app.py` right after `app = Flask(__name__)`:

```python
@app.template_filter("enumerate")
def _enumerate_filter(iterable, start=0):
    return enumerate(iterable, start=start)
```

- [ ] **Step 5: Run all tests**

```bash
pytest tests/ -v
```

Expected: all tests pass (12 existing + new ones in test_app.py).

- [ ] **Step 6: Verify manually**

```bash
python app.py
```

Upload a real Freedom24 JSON file at http://localhost:5000 and verify the results page renders correctly with two columns and correct numbers.

- [ ] **Step 7: Commit**

```bash
git add app.py templates/result.html tests/test_app.py
git commit -m "feat: add calculate route and result page"
```

---

## Chunk 3: README update

### Task 4: Update README.md

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Verify run instructions are accurate**

Before writing, confirm in a clean shell:

```bash
source .venv/bin/activate
python app.py
```

Expected: `* Running on http://127.0.0.1:5000` — no import errors.
Open http://localhost:5000 — upload page loads. Then Ctrl+C.

- [ ] **Step 2: Update README.md**

Replace or rewrite the relevant sections. The README must contain at minimum:


```markdown
# Калькулятор податків Freedom24

Локальний інструмент для розрахунку ПДФО та ВЗ з виписки Freedom24 брокера.
Допомагає заповнити рядок 10.10 та Додаток Ф1 декларації (cabinet.tax.gov.ua).

## Запуск

```bash
python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Відкрий http://localhost:5000 у браузері.

## Як отримати файл з Freedom24

1. Відкрий freedom24.com → Мій рахунок → Звіти → Загальний звіт
2. Формат: **JSON**
3. Період: **від дати відкриття рахунку** до 31 грудня звітного року
   > Повна історія потрібна для правильного FIFO-розрахунку

## Дисклеймер

Інструмент надається виключно для довідки. Перевірте розрахунки самостійно
або з податковим консультантом. Автор не несе відповідальності за помилки.

## Тести

```bash
pytest
```
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: update README for Flask web app"
```

---

## Final verification

- [ ] Run full test suite: `pytest -v` — all tests pass
- [ ] Start app: `python app.py`
- [ ] Open http://localhost:5000 — upload page shows correctly
- [ ] Upload a Freedom24 JSON — results page renders with correct totals and instruction tables
- [ ] Click "← Розрахувати інший файл" — returns to upload page
- [ ] Submit empty form — flash error appears on upload page
